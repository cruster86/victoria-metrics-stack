{{- if .Values.vmstaticscrape.enabled -}}
{{- $top := . }}
{{- range .Values.vmstaticscrape.config }}
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMStaticScrape
metadata:
  name: vmstaticscrape-{{ .name $top }}
spec:
  jobName: {{ .name $top }}
  targetEndpoints:
{{ toYaml .targetEndpoints $top | indent 4 }}
{{- end }}
{{- end }}